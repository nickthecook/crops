dependencies:
  gem:
    - rerun
  custom:
    - true
    - echo "Initializing the cloud..."
actions:
  run: crystal run ops.cr --
  run-debug: crystal run -d --error-trace ops.cr --
  run-watch:
    command: rerun -qbx -p '**/*.cr' -- ops run
    alias: rw
  test: crystal spec
  test-watch:
    command: rerun -qbx -p '**/*.cr' -- ops test
    alias: tw
  sayhi:
    command: echo 'Hello, world!'
    alias: hi
