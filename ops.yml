dependencies:
  gem:
    - rerun
  apk:
    - less
  apt:
    - less
  custom:
    - true
    - echo "Initializing the cloud..."
  dir:
    - dirsftw
  sshkey:
    - id_rsa_nick
actions:
  run: crystal run ops.cr --
  run-debug: crystal run -d --error-trace ops.cr --
  run-watch:
    command: rerun -qb -p '**/*.cr' -- ops run
    alias: rw
  test: crystal spec
  test-watch:
    command: rerun -qb -p '**/*.cr' -- ops test
    alias: tw
  sayhi:
    command: echo 'Hello, world!'
    alias: hi
options:
  sshkey:
    passphrase_var: SSH_KEY_PASSPHRASE
